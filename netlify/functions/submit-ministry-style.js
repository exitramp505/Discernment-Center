const { createClient } = require('@supabase/supabase-js');
const STATE_REGIONS={WA:'Pacific',HI:'Pacific',AK:'Pacific',AZ:'Pacific',UT:'Pacific',CA:'Pacific',NV:'Pacific',ID:'Pacific',OR:'Pacific',TX:'Central',OK:'Central',AR:'Central',WI:'Central',MN:'Central',IA:'Central',IL:'Central',MO:'Central',KS:'Central',CO:'Mountain Plains',WY:'Mountain Plains',NE:'Mountain Plains',SD:'Mountain Plains',ND:'Mountain Plains',MT:'Mountain Plains',NH:'East',VT:'East',MA:'East',ME:'East',RI:'East',CT:'East',NJ:'East',DE:'East',MD:'East',WV:'East',PA:'East',OH:'East',VA:'East',KY:'East',TN:'East',IN:'East',MI:'East',NY:'East',FL:'South East',GA:'South East',AL:'South East',MS:'South East',LA:'South East',SC:'South East',NC:'South East',PR:'South East'};
function json(statusCode,body){return {statusCode,headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}}function adminClient(){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return null;return createClient(url,key,{auth:{autoRefreshToken:false,persistSession:false}})}async function userFromEvent(event,admin){const h=event.headers.authorization||event.headers.Authorization||'',token=h.startsWith('Bearer ')?h.slice(7):'';if(!token)return null;const {data,error}=await admin.auth.getUser(token);if(error||!data?.user)return null;return data.user}
exports.handler=async(event)=>{if(event.httpMethod!=='POST')return json(405,{ok:false,error:'Method not allowed'});try{const admin=adminClient();if(!admin)return json(500,{ok:false,error:'Supabase service role key is not configured.'});const user=await userFromEvent(event,admin);if(!user)return json(401,{ok:false,error:'You must be logged in to submit the inventory.'});const body=JSON.parse(event.body||'{}'),candidate=body.candidate||{},scores=body.scores||{},answers=body.answers||{},state=candidate.state||'',region=candidate.region||STATE_REGIONS[state]||'';if(scores.assessmentType!=='ministry_style')scores.assessmentType='ministry_style';scores.assessmentTitle=scores.assessmentTitle||'Ministry Style Inventory';await admin.from('candidate_profiles').upsert({id:user.id,full_name:candidate.name||user.user_metadata?.full_name||user.user_metadata?.name||user.email||'',email:candidate.email||user.email||'',phone:candidate.phone||'',state,region,married:candidate.married||'',updated_at:new Date().toISOString()},{onConflict:'id'});const {data,error}=await admin.from('assessment_results').insert({user_id:user.id,candidate,scores,answers,state,region,overall:Number(scores.overall)||null,overall_label:scores.overallLabel||scores.blendName||'',routed_leader:'',email_sent:false,blob_key:'',legacy_submission_id:''}).select('id').single();if(error)throw error;return json(200,{ok:true,id:data.id})}catch(err){return json(500,{ok:false,error:err.message||'Could not submit Ministry Style Inventory.'})}};