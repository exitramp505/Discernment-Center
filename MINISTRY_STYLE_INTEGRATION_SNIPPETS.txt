MINISTRY STYLE INVENTORY INTEGRATION SNIPPETS

1) Add assignment item in admin.js ASSIGNMENT_ITEMS:
{key:'ministry_style', label:'Style', fullTitle:'Ministry Style Inventory', type:'assessment'},

Recommended order:
Ministry Readiness Inventory
Ministry Style Inventory
Discernment Center Application
Character Qualities Assessment
Pastoral Reference Form

2) In dashboard.js, identify report:
const ministryStyleReport = allReports.find(r => r.scores?.assessmentType === 'ministry_style');

Make sure character report excludes ministry_style:
const characterReport = allReports.find(r => !['isa_readiness','ministry_style'].includes(r.scores?.assessmentType || ''));

Add card when assigned:
assignedKeys.has('ministry_style') ? assessmentCard({
  title: 'Ministry Style Inventory',
  description: 'DISC-informed ministry behavior inventory focused on leadership, communication, team dynamics, and church multiplication.',
  report: ministryStyleReport,
  draftKey: `ministry_style_draft_${(profile?.email || user.email || '').toLowerCase()}`,
  startUrl: 'ministry-style.html',
  retakeUrl: 'ministry-style.html',
  unit: ''
}) : ''

3) In admin-submissions.js normalizeReport, title logic should include:
s.assessmentType === 'ministry_style' ? 'Ministry Style Inventory' : ...

4) In admin.js assessmentTitle should include:
if (r.assessmentType === 'ministry_style') return 'Ministry Style Inventory';

5) Optional report rendering:
Load ministry-style-report.js on admin.html before admin.js, then in reportHtml(record):
if ((record.scores||{}).assessmentType === 'ministry_style') return MinistryStyleReport.buildReportHtml(record);
